import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages project site URL is https://<user>.github.io/Practice/
  // so we set the base path to the repository name.
  base: '/Practice/',
});
